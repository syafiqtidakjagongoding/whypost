{
  "name": "whypost-app",
  "version": "1.0.0",
  "description": "A Fediverse client for mobile.",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky"
  },
  "lint-staged": {
    "lib/**/*.dart": "flutter analyze",
    ".github/workflows/*.{yml,yaml}": "npx yaml-lint",
    "*.{yml,yaml}": "npx yaml-lint"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^20.2.0",
    "@commitlint/config-conventional": "^20.2.0",
    "husky": "^8.0.0",
    "lint-staged": "^16.2.7",
    "yaml-lint": "^1.7.0"
  }
}
